# URL del backend (Express). In locale e' http://localhost:3001.
VITE_API_URL=http://localhost:3001

# Supabase (OPZIONALE: solo per login + watchlist). Usa SOLO la anon key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
