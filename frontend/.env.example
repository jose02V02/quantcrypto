# URL del backend.
# - Su Vercel (funzioni serverless /api/*): LASCIA VUOTO (same-origin).
# - In locale con il backend Express: http://localhost:3001
VITE_API_URL=http://localhost:3001

# Supabase (OPZIONALE: solo per login + watchlist). Usa SOLO la anon key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
